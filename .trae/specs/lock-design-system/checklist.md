# Checklist

- [x] `OpedrgentTheme` 的 `dynamicColor` 默认值为 `false`
- [x] 设置页存在「动态主题」开关且可持久化
- [x] `ui/theme/Shape.kt` 已创建并包含全部圆角 token
- [x] `ui/theme/Spacing.kt` 已创建并包含全部间距 token
- [x] 语义颜色可通过主题或 CompositionLocal 读取
- [x] `ThemePreview.kt` 提供了浅色/深色色板预览
- [x] `./gradlew assembleDebug` BUILD SUCCESSFUL
