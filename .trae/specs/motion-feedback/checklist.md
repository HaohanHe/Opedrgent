# Checklist

- [x] 存在统一的 `showFeedback` 入口
- [x] `ui/` 包内无 `Toast.makeText`
- [x] `ui/components/` 包内无 `Toast.makeText`
- [x] 核心组件有 `@Preview`（含深色模式）
- [x] `EmptyStateView` 支持插画 + 标题 + 描述 + 行动按钮
- [x] 笔记列表、知识库、聊天消息列表有进入/退出动画
- [x] `HoldToDictate` 有震动 + 环形进度 + 释放反馈
- [x] 流式输出光标为脉冲动画
- [x] `./gradlew assembleDebug` BUILD SUCCESSFUL
